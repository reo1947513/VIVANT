/**
 * テスト実行中だけ server-only の代わりに読み込まれる、中身の無いファイル。
 *
 * 本物の server-only は「ブラウザ側から読み込まれたらエラーにする」ためのもので、
 * サーバー専用のファイルが誤って画面側へ混ざるのを防いでいる。
 * テストはサーバーでもブラウザでもない場所で動くため、そのままだとこの印だけで
 * 失敗してしまう。ここで空に差し替えることで、印を無効にしたうえで中身を確認する。
 *
 * 差し替えは vitest.config.ts の中だけで行われ、本番の動きには影響しない。
 */
export {};
