# コミットの直前に走る確認。
#
# ここでは軽いものだけにしている。テストは32件で約0.2秒なので、
# 毎回のコミットに挟んでも待たされる感覚がない。
# 型チェックとビルドは時間がかかるため push の直前（.husky/pre-push）で行う。
#
# どうしても止めたいときは git commit --no-verify で飛ばせるが、
# その場合は push 時の確認で結局止まる。

echo "▶ テストを実行します（32件）"
npm test
